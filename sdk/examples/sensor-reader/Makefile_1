# Makefile
CC = arm-poky-linux-gnueabi-gcc
CFLAGS = -O2 -Wall -g
TARGET = hello
SOURCES = hello.c

all: $(TARGET)

$(TARGET): $(SOURCES)
	$(CC) $(CFLAGS) -o $@ $^

clean:
	rm -f $(TARGET)

install:
	cp $(TARGET) /usr/bin/
