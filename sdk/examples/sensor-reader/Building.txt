# Source SDK environment
source sdk/environment-setup.sh

# Build with make
make

# Build with cmake
mkdir build && cd build
cmake -DCMAKE_TOOLCHAIN_FILE=../../sdk/cmake/Toolchain.cmake ..
make

# Deploy to target
scp hello root@<board-ip>:/tmp/

# Run on target
ssh root@<board-ip> /tmp/hello
