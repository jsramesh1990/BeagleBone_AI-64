# This file is automatically generated and contains auto-detected configuration
# You should not modify this file manually

# ============================================================================
# Auto-detected Settings
# ============================================================================
# These settings are automatically detected based on your system configuration
# and should not be modified here

# System information
BUILD_SYS = "x86_64-linux"
TARGET_SYS = "aarch64-poky-linux"

# Toolchain information
TUNE_ARCH = "aarch64"
TUNE_PKGARCH = "aarch64"
TUNE_FEATURES = "aarch64"

# Machine-specific settings
MACHINE_FEATURES:append = " bbbai64"

# SDK settings
SDK_ARCH = "x86_64"

# Package architecture settings
PACKAGE_ARCHS = "all any noarch ${TUNE_PKGARCH}"

# ============================================================================
# Auto-detected Paths
# ============================================================================
# These paths are automatically detected and should not be modified manually

# Toolchain paths
TARGET_CC_ARCH = "${TUNE_CCARGS}"
TARGET_CFLAGS = "${TARGET_CC_ARCH} ${TARGET_CPPFLAGS}"
TARGET_LDFLAGS = "${TARGET_CC_ARCH}"

# Linker settings
LD = "${CCACHE}${TARGET_PREFIX}ld"
AR = "${TARGET_PREFIX}ar"
AS = "${TARGET_PREFIX}as"
RANLIB = "${TARGET_PREFIX}ranlib"
STRIP = "${TARGET_PREFIX}strip"
OBJCOPY = "${TARGET_PREFIX}objcopy"
OBJDUMP = "${TARGET_PREFIX}objdump"

# ============================================================================
# End of auto.conf
# ============================================================================
